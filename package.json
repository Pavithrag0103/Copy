{
  "name": "rasengan",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test_o": "npx playwright test --config=playwright.config.ts",
    "test": "npx playwright test --config=playwright.config.ts && npm run report:allure",
    "test:dev": "ENV=dev playwright test",
    "posttest": "npx allure generate allure-results --clean && npx allure open",
    "clean:allure": "rmdir /s /q allure-results && rmdir /s /q allure-report",
    "report:allure": "npx allure generate allure-results --clean -o allure-report && npx allure open allure-report"
  },
  "repository": {
    "type": "git",
    "url": "https://Syneos-SSP@dev.azure.com/Syneos-SSP/Rasengan/_git/Rasengan"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^22.15.1",
    "@types/oracledb": "^6.6.0",
    "@types/pngjs": "^6.0.5",
    "@types/xlsx": "^0.0.35",
    "allure-commandline": "^2.34.1",
    "allure-playwright": "^3.3.2"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "oracledb": "^6.8.0",
    "pixelmatch": "^7.1.0",
    "playwright": "^1.52.0",
    "pngjs": "^7.0.0",
    "winston": "^3.17.0",
    "xlsx": "^0.18.5"
  }
}
