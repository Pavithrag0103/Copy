import { ExcelUtils } from '../utils/excel';
import { ReportUtils } from '../utils/report';
import {PlaywrightExtension} from "../utils/playwright-extension"

export const excelUtils = new ExcelUtils();
export const reportUtils = new ReportUtils();
export const playwrightExtension = new PlaywrightExtension();
